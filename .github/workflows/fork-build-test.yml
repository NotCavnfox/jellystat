# Fork CI gate. Calls the shared reusable workflow in NotCavnfox/.github.
name: fork-build-test

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: NotCavnfox/.github/.github/workflows/build-test.yml@main
    permissions:
      contents: read
      packages: write
    with:
      image_name: ghcr.io/notcavnfox/jellystat
      smoke_port: 3000
      smoke_path: /
      run_smoke: false   # needs Postgres to boot — validated via compose integration
      compose_test: true
      publish_edge: true
